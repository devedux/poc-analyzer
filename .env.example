# Modelo de Ollama a usar (debe estar descargado: ollama pull llama3.2)
OLLAMA_MODEL=llama3.2

# Rutas a los repos del POC (relativas o absolutas)
FRONT_REPO_PATH=../poc-front-app
E2E_REPO_PATH=../poc-e2e-tests

# GitHub Personal Access Token (permisos: repo)
GITHUB_TOKEN=ghp_...

# Neo4j — Knowledge Graph (Step 4)
# Levantar con: docker compose up -d
# Browser: http://localhost:7474 (user: neo4j, pass: password)
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=password

# Neo4j separada para tests de integración (recomendado para no contaminar producción)
# Sin estas vars, los tests usan NEO4J_URI y limpian via deleteTestNodes.
# Para aislamiento total: docker run -d -p 7688:7687 -p 7475:7474 \
#   -e NEO4J_AUTH=neo4j/password --name neo4j-test neo4j:5
# NEO4J_TEST_URI=bolt://localhost:7688
# NEO4J_TEST_USER=neo4j
# NEO4J_TEST_PASSWORD=password
